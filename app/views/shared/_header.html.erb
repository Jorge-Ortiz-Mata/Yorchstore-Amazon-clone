<nav>
    <ul>
        <li>
            <%= link_to "Yorchstore", root_path %>
        </li>
    <% if !current_user %>
        <li>
            <%= link_to "Sign in", new_user_session_path, class:"block mt-4 lg:inline-block lg:mt-0 text-white hover:text-cyan-200 mr-4" %>
        </li>
        <li>
            <%= link_to "Sign up", new_user_registration_path, class:"block mt-4 lg:inline-block lg:mt-0 text-white hover:text-cyan-200 mr-4" %>
        </li>
    <% else %>
        <%# if current_user.profile.present? %>
            <li class="mt-auto mb-auto">
                <%#= link_to "@ #{current_user.profile.first_name} #{current_user.profile.last_name}", profile_path(current_user.profile), class:"block mt-4 lg:inline-block lg:mt-0 text-white hover:text-cyan-200 mr-4" %>
            </li>
        <%# end %>
            <li class="mt-auto mb-auto">
                <%= link_to "Sign out", destroy_user_session_path, "data-turbo-method": :delete, class:"block mt-4 lg:inline-block lg:mt-0 text-white hover:text-cyan-200 mr-4" %> 
            </li>
        <% end %>
    </ul>
</nav>